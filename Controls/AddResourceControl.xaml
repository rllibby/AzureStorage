<UserControl
    x:Class="AzureStorage.Controls.AddResourceControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" x:Name="ThisPage"
    d:DesignHeight="150"
    d:DesignWidth="500">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" BorderBrush="{StaticResource CustomColorBrush}" BorderThickness="2" >

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Add resource form -->
        <StackPanel x:Name="editPanel" Grid.Row="1" Margin="20, 16" DataContext="{Binding ElementName=ThisPage}">
            <TextBox x:Name="nameText" Header="{Binding Resource.NewDescription, Mode=OneWay}" IsTextPredictionEnabled="False" IsSpellCheckEnabled="False" Text="{Binding Resource.Name, Mode=TwoWay}" TextChanged="OnTextChanged" KeyUp="OnKeyUp"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="addButton" Margin="0,20,8,0" Command="{Binding Resource.Add}" HorizontalAlignment="Left">Add</Button>
                <Button x:Name="cancelButton" Margin="0,20,0,0" Click="CloseClicked" HorizontalAlignment="Left">Cancel</Button>
            </StackPanel>
        </StackPanel>
    </Grid>

</UserControl>
